apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ci-cd-demo-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ci-cd-demo-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ci-cd-demo-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
